/**
 * Enum for GuestType.
 * @readonly
 * @enum {Number}
 */
const GuestType = Object.freeze({
    BOY: 0,
    GIRL: 1,
    VIP: 2
})

/**
 * Enum for AngryStage.
 * @readonly
 * @enum {Number}
 */
const AngryStage = Object.freeze({
    A_NORMAL: 0,
    B_NERVOUS: 1,
    C_RAGED: 2,
    HEART: 3
});

/**
 * Enum for CookStage.
 * @readonly
 * @enum {Number}
 */
const CookStage = Object.freeze({
    EMPTY: 0,
    KIJI: 1,
    INSIDE: 2,
    SINGLEFLIP: 3,
    DOUBLEFLIP: 4,
    BURNED: -1
});

/**
 * Enum for TaiyakiType.
 * @readonly
 * @enum {Number}
 */
const TaiyakiType = Object.freeze({
    ANKO: 0,
    CREAM: 1,
    CHOCO: 2
});

/**
 * Enum for GuestType.
 * @readonly
 * @enum {Number}
 */
const AnimationType = Object.freeze({
    TRANSITION: 0,
    ALPHA: 1
});

/**
 * Enum for EasingType.
 * @readonly
 * @enum {Number}
 */
const EasingType = Object.freeze({
    DEFAULT: 0,
    EASING: 1
});